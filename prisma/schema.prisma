// Prisma Schema
// TODO: Implement in Phase 3 - Database & ORM Setup
// - Define User model for Better Auth
// - Define Session model for authentication
// - Define trading-related models (Account, Trade, etc.)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
}

// TODO: Define models in Phase 3
// model User {
//   id            String    @id @default(cuid())
//   email         String    @unique
//   emailVerified Boolean
//   name          String?
//   createdAt     DateTime  @default(now())
//   updatedAt     DateTime  @updatedAt
//   sessions      Session[]
// }
//
// model Session {
//   id        String   @id @default(cuid())
//   userId    String
//   user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
//   expiresAt DateTime
//   createdAt DateTime @default(now())
//   updatedAt DateTime @updatedAt
// }
